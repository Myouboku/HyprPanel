import { opt } from 'src/lib/options';

export default {
    label: opt(true),
    showDeviceNameWhenSingle: opt(true),
    rightClick: opt(''),
    middleClick: opt(''),
    scrollUp: opt(''),
    scrollDown: opt(''),
};
